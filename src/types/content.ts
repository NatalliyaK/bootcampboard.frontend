export type AddContentBlock = {
  title: string;
  text: string;
};

export type ContentData = {
  id: string;
  title: string;
  text: string;
};

export type EditTitleBlock = {
  id: string;
  title: string;
};
